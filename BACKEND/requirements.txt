# FastAPI + ASGI
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
oracledb==2.3.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validation / Form data
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.26

# HTTP client (ViaCEP, ML API, Shopee API)
httpx==0.28.1

# WebSocket / Socket.io
python-socketio==5.11.4

# Background jobs
apscheduler==3.10.4

# XML parsing (NFe import)
xmltodict==0.13.0

# Etiqueta de pedido manual (PDF + barcode)
reportlab>=4.0.0
python-barcode>=0.15.1
Pillow>=10.0.0

# Exportação de relatórios em Excel (Vendas do Mês)
openpyxl>=3.1.0

# Certificado A1 (.pfx) — leitura PKCS#12 + senha cifrada (Fernet) no banco
cryptography>=42.0.0

# Emissão própria de NF-e (SEFAZ direto): XML 4.00, assinatura XMLDSig SHA-1, SOAP mTLS
lxml>=5.0
signxml>=3.2
urllib3>=2.0

# DANFE/DANFCE em PDF a partir do XML autorizado
brazilfiscalreport>=1.0

# CORS
# (built into FastAPI/Starlette)

# Dev / Quality / Testing
ruff>=0.4.0
mypy>=1.10.0
types-python-jose>=3.3.0
types-passlib>=1.7.7
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.28.1
